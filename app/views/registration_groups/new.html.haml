- title t('title.registration_group')
.auto_focus

= semantic_form_for @registration_group do |form|
  = render 'form', :form => form
  = form.actions do
    = form.action(:submit, :label => :new_registration_group, :button_html => {:tabindex => autotab})
