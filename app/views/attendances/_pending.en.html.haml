%h3 Instructions to complete the registration process

= render partial: 'shared/payment_request', locals: { attendance: @attendance }

%hr/

%h3 Important

- deadline = attendance.registration_date.to_date + 5
%p After #{I18n.l([attendance.registration_period.end_at.to_date, deadline].max)}, the registration amount will be updated according to the table available at #{link_to price_table_link(attendance.event, :en), price_table_link(attendance.event, :en)}.

%div.actions
  - if can? :confirm, Attendance
    = button_to 'Confirm registration', confirm_attendance_path(attendance), method: :put, data: { confirm: t('attendance.confirm_confirmation', event: attendance.event.name) }
  = button_to 'Cancel registration', attendance, method: :delete, class: 'remove', data: { confirm: t('attendance.cancel_confirmation', event: attendance.event.name ) }
