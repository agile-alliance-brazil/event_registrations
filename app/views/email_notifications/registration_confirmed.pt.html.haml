Oi #{h(@attendance.full_name)},

%p Sua inscrição na #{@attendance.event_name} foi confirmada.

%ul
  - if @attendance.event.start_date.present? && @attendance.event.end_date.present?
    - if @attendance.event.start_date == @attendance.event.end_date
      %li Quando: #{ l(@attendance.event.start_date.to_date) }
    - else
      %li Quando: #{ l(@attendance.event.start_date.to_date) } #{ t('general.until')} #{l(@attendance.event.end_date.to_date) }

%p Qualquer dúvida, entre em contato através do email #{@attendance.event.main_email_contact}

%small Por favor não responda este e-mail. Ele foi enviado através de um endereço não monitorado
