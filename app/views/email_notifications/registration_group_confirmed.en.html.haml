%p Dear #{h(@registration_group.contact_name)},

%p The registration of company <strong>#{h(@registration_group.name)}</strong> priced #{number_to_currency(@registration_group.registration_fee, :locale => :pt)} at #{@event_name} was confirmed successfully. The registration includes:

%ul
  - @registration_group.attendees.each do |attendee|
    %li
      #{h(attendee.full_name)} priced #{number_to_currency(attendee.registration_fee, :locale => :pt)}

%p If you have any questions, please contact us at #{mail_to APP_CONFIG[:organizer][:contact_email]}

%small [Please do not reply to this e-mail. It was sent from an unmonitored email address]