%ul#menu
  = link_to_menu_item(:li, t('title.events'), events_path)
  - if current_user
    = link_to_menu_item(:li, t('actions.logout'), logout_path)
  - else
    = link_to_menu_item(:li, t('actions.login'), login_path)
