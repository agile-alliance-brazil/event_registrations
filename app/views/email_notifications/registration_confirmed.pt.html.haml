Oi #{h(@attendance.full_name)},

%p Sua inscrição na #{@attendance.event.name} foi confirmada.

%ul
  - if @attendance.event.start_date.present? && @attendance.event.end_date.present?
    - if @attendance.event.start_date == @attendance.event.end_date
      %li Quando: #{ l(@attendance.event.start_date.to_date) }
    - else
      %li Quando: #{ l(@attendance.event.start_date.to_date) } #{ t('title.until')} #{l(@attendance.event.end_date.to_date) }

  %li Informações sobre como chegar: http://www.agilebrazil.com/2015/localizacao.html

%p Qualquer dúvida, entre em contato através do email #{mail_to APP_CONFIG[:organizer][:contact_email]}

%small [Por favor não responda este e-mail. Ele foi enviado através de um endereço não monitorado]
