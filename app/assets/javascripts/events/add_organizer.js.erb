function submitOrganizer(event_id) {
  var email = $('#email').val();

  $.ajax({
    type: "PATCH",
    url: '/events/' + event_id + '/add_organizer',
    data: { _method: 'PATCH', email: email }
  });
}
