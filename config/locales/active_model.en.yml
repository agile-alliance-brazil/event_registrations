en:
  errors:
    format: "%{attribute} %{message}"
    messages:
      accepted: must be accepted
      blank: This field can't be blank
      present: must be blank
      confirmation: doesn't match %{attribute}
      empty: can't be empty
      equal_to: must be equal to %{count}
      even: must be even
      exclusion: is reserved
      greater_than: must be greater than %{count}
      greater_than_or_equal_to: must be greater than or equal to %{count}
      inclusion: is not included in the list
      invalid: is invalid
      less_than: must be less than %{count}
      less_than_or_equal_to: must be less than or equal to %{count}
      model_invalid: "Validation failed: %{errors}"
      not_a_number: is not a number
      not_an_integer: must be an integer
      odd: must be odd
      required: must exist
      taken: has already been taken
      too_long:
        one: is too long (maximum is 1 character)
        other: is too long (maximum is %{count} characters)
      too_short:
        one: is too short (minimum is 1 character)
        other: is too short (minimum is %{count} characters)
      wrong_length:
        one: is the wrong length (should be 1 character)
        other: is the wrong length (should be %{count} characters)
      other_than: must be other than %{count}
      same_advisor: "You cannot book a session with yourself"
    template:
      body: 'There were problems with the following fields:'
      header:
        one: 1 error prohibited this %{model} from being saved
        other: "%{count} errors prohibited this %{model} from being saved"
  activerecord:
    errors:
      messages:
        record_invalid: 'Validation failed: %{errors}'
        restrict_dependent_destroy:
          has_one: Cannot delete record because a dependent %{record} exists
          has_many: Cannot delete record because dependent %{record} exist
    attributes:
      invoice:
        payment_types:
          gateway: Gateway
          bank_deposit: Bank Deposit
          statement_agreement: Statement of Agreement
        payment_type: "Payment type"
      attendance:
        full_name: "Name"
        status: "Status"
        organization_size: "Organization size"
        organization: "Organization"
        phone: "Phone"
        email: "Email"
        state: "State"
        city: "City"
        country: "Country"
        first_name: "Name"
        last_name: "Last Name"
        badge_name: "Badge Name"
        gender: "Gender"
        discount: "Discount"
        twitter_user: User on twitter
        default_locale: Default Locale
        job_role: "Job role"
        years_of_experience: "Years of Experience"
        experience_in_agility: "Experience in Agility"
        school: "School"
        education_level: "Education Level"
      user:
        roles: Roles
        email: Email
        name: Name
  invoices:
    show:
      title: "Invoice"
  attendance:
    enum:
      status:
        waiting: "In line"
        pending: "Pending"
        accepted: "Accepted"
        paid: "Paid"
        cancelled: "Cancelled"
        confirmed: "Confirmed"
        showed_in: "Accredited"
      education_level:
        primary: "Primary Education"
        secondary: "Secondary Education"
        tec_secondary: "Post-secondary non-tertiary education"
        tec_terciary: "Short-cycle tertiary education"
        bachelor: "Bachelor or equivalent"
        master: "Master"
        doctoral: "Doctoral"
  registration_group:
    paid_in_advance: "Reserved"
  event:
    average_ticket: "Average Ticket"