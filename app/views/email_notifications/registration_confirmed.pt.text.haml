Oi #{h(@attendance.full_name)},



Sua inscrição na #{@attendance.event.name} foi confirmada.


- if @attendance.event.start_date.present? && @attendance.event.end_date.present?
  - if @attendance.event.start_date == @attendance.event.end_date
    Quando: #{ l(@attendance.event.start_date.to_date) }
  - else
    Quando: #{ l(@attendance.event.start_date.to_date) } #{ t('title.until')} #{l(@attendance.event.end_date.to_date) }


Informações sobre como chegar: #{@attendance.event.location_and_date}



Qualquer dúvida, entre em contato através do email #{APP_CONFIG[:organizer][:contact_email]}



[Por favor não responda este e-mail. Ele foi enviado através de um endereço não monitorado]
